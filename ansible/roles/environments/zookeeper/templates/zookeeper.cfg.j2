tickTime=2000
initLimit=10
syncLimit=5
dataDir={{ zookeeper_data_dir }}
clientPort={{ zookeeper_client_port }}
{% for host in groups['zookeeper'] %}
server.{{ loop.index }}={{ host }}:{{ zookeeper_peer_port }}:{{ zookeeper_quorum_port }}
{% endfor %}
