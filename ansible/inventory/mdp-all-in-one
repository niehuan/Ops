[mdp_node]
localhost

[data_node]
localhost

[client_node]
localhost

[master_node]
localhost

[kafka:children]
data_node

[zookeeper:children]
data_node

[elasticsearch:children]
data_node

[repo_server]
localhost

[gems_server:children]
# 多节点部署时填localhost，127.0.0.1会引发错误
repo_server

[yum_repo_server:children]
# 多节点部署时填localhost，127.0.0.1会引发错误
repo_server

[mysql:children]
mdp_node

[mdp:children]
yum_repo_server
gems_server
repo_server
elasticsearch
zookeeper
kafka
mysql
master_node
client_node
data_node
mdp_node
